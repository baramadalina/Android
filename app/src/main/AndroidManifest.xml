<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidtutorialshub.loginregister">

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:label="@string/app_name">
        <activity
            android:name=".activities.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegisterActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.UsersListActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.SecondActivity" />
        <activity android:name=".activities.InventoryMainActivity" />
        <activity android:name=".activities.InventoryUpdateDeleteActivity" />
        <activity android:name=".activities.TasksActivity" />
        <activity android:name=".activities.InventoryCommentsActivity" />

        <activity
            android:name=".activities.InventoryListActivity"
            android:configChanges="orientation"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@menu/menu_search"></meta-data>
        </activity>
        <activity android:name=".activities.CalendarActivity" />
        <activity android:name=".activities.AddReservationEventActivity" />
        <activity android:name=".activities.demo.MenuActivity" />
        <activity android:name=".activities.demo.HomeActivity" />
        <activity android:name=".activities.ReservationEventDetailsActivity" />
        <activity android:name=".activities.ActivitySendEmail" />

    </application>

</manifest>